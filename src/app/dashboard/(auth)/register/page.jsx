import React from 'react'

export const metadata = {
    title: 'Register',
    description: 'Blog Application',
}

const Register = () => {
    return (
        <div>Register</div>
    )
}

export default Register